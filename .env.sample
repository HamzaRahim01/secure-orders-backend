# Django Settings
# SECRET_KEY: A unique and secure key for Django (generate with `python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"`).
# Do not commit the actual value to version control.
SECRET_KEY=your-secure-random-key-here

# DEBUG: Set to 0 for production, 1 for development.
# Warning: Debug mode exposes sensitive information; use 0 in production.
DEBUG=0

# DJANGO_ALLOWED_HOSTS: Space-separated list of allowed hostnames.
# Example: localhost 127.0.0.1 [::1] for local development.
DJANGO_ALLOWED_HOSTS=localhost 127.0.0.1 [::1]

# PostgreSQL Database Configuration
# POSTGRES_DB: Name of the database.
POSTGRES_DB=

# POSTGRES_USER: Username for PostgreSQL.
POSTGRES_USER=

# POSTGRES_PASSWORD: Password for the PostgreSQL user.
# Replace with a strong, unique password.
POSTGRES_PASSWORD=your-secure-password-here

# POSTGRES_HOST: Hostname for the database (set to 'db' for Docker service name).
POSTGRES_HOST=

# POSTGRES_PORT: Port for PostgreSQL (default is 5432).
POSTGRES_PORT=